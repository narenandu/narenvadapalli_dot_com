version: 1
frontend:
  phases:
    preBuild:
      commands:
        # Install nvm and Node.js 23 (Astro 5+ requires Node >= 22.12.0)
        - curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.7/install.sh | bash
        - . ~/.nvm/nvm.sh && nvm install 23 && nvm use 23 && npm ci
    build:
      commands:
        # Source nvm and build the project using Node.js 23
        - . ~/.nvm/nvm.sh && nvm use 23 && npm run build
  artifacts:
    # Astro builds static files to the 'dist' directory
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
